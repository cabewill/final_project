---
- name: Server Setup
  hosts: localhost
  become: true
  tasks:
    - name: Install required packages
      apt:
        name:
          - sqlite3
          - nginx
          - git
          - python3
          - python3-pip
        state: present

    - name: Enable and start Nginx
      service:
        name: nginx
        state: started
        enabled: true
